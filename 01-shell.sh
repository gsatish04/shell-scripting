#!/bin/bash

# #! is called as She-Bang , It denotes the path of interpreter and also ensure the script executes with that interpreter if you executes the script ./script.sh instead of bash script.sh

# Any line starts with # is a comment and those lines will be ignored during execution.

 ls

 ls -ltr
